# Backend URL Configuration
# 
# For production builds, set VITE_BACKEND_URL to your backend API URL.
# This variable is embedded at build time, so changes require a rebuild.
#
# Examples:
# VITE_BACKEND_URL=http://localhost:5000
# VITE_BACKEND_URL=https://api.your-domain.com
# VITE_BACKEND_URL=https://backend.math2visual.example.com

# Production backend URL (set this before building for production)
VITE_BACKEND_URL=http://localhost:5000

# Development backend URL (used by Vite dev server proxy)
# This is optional - defaults to http://localhost:5000 in vite.config.ts
# BACKEND_URL=http://localhost:5000
